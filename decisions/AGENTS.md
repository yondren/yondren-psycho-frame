# decisions/ 子树指令

- 写入前先读 [README.md](README.md)。
- 每次非平凡变更必须新增或更新至少一条决策记录，与代码/文档同一提交。
- 只改事实不改决策；决策反转必须新记录 + 交叉链接。
- `Status:` 语法与生命周期一致；`archived/` 保留 `Status: implemented` 并加 `Archived:` 行，归档后冻结。
- 结构与格式由 `pnpm verify:docs` 门禁校验。
