# 两包锁步发布 0.5.0 与发布准备入口

- task_key: release-050
- status: DONE
- created: 2026-09-17
- updated: 2026-09-17
- report: [reports/release-050.md](../reports/release-050.md)

## 范围

本次包含：只准备入口 `pnpm release:prepare`（`--prepare` 与 `--publish`/`--push` 互斥）、
`--task-key=` 参数、发布交接块输出、`--push` 补 codeup 镜像；发布手册
[docs/cookbook/release.md](../docs/cookbook/release.md)；0.5.0 版本准备（bump 两包与模板 pin
→ verify/doctor → pack 预检 → commit + tag `v0.5.0`）与发布后收尾。

本次不包含：`packages/` 运行时代码变更、npm 自动发布、`npm publish` 本身（由维护者持凭据与
2FA 执行）。

## 进度

- [DONE] 2026-09-17 发布准备入口、交接块与手册落地（`8ba4fab`，已推 origin main + codeup mirror）。
- [DONE] 2026-09-17 0.5.0 准备：`59a07d9` + 注解 tag `v0.5.0`，pack 预检通过。
- [DONE] 2026-09-17 维护者双发与推送完成；registry、tarball 与消费端（`npx` 与 `npm create` 路径）
  核对通过，首次 PUT E404 判定为 token 失效并已重登解决。
