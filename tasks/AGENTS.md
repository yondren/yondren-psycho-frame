# tasks/ 子树指令

- 任务状态真源在本目录，规则见 [README.md](README.md)。
- 开工前登记 `tasks/yyyy-mm-dd-task_key.md` 并置 `IN_PROGRESS`；微小改动豁免，提交前补登记。
- 完成顺序：验证 → 报告 → 文档/决策同步 → 提交（含 task_key）→ 置 `DONE`。
- 头字段与 task_key 一致性由 `pnpm verify:docs` 校验。
